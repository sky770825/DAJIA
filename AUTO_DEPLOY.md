# 🚀 Cloudflare Pages 自动化部署

## 一键部署命令

### 预览部署（推荐用于测试）
```bash
npm run deploy:cloudflare
```

### 生产环境部署
```bash
npm run deploy:cloudflare:prod
```

## 自动化脚本功能

`deploy-cloudflare.sh` 脚本会自动处理：

1. ✅ **检查 wrangler CLI** - 如果未安装会自动安装
2. ✅ **检查登录状态** - 如果未登录会自动引导登录
3. ✅ **构建项目** - 自动运行 `npm run build`
4. ✅ **检查构建输出** - 验证 `dist/` 目录和文件
5. ✅ **确保 _redirects 文件** - 自动检查并复制 SPA 路由配置
6. ✅ **部署到 Cloudflare Pages** - 自动部署到指定项目

## 首次使用

第一次运行时，脚本会引导您：

1. **安装 wrangler**（如果需要）
   ```bash
   npm install -g wrangler
   ```

2. **登录 Cloudflare**
   ```bash
   wrangler login
   ```
   按照提示在浏览器中完成登录。

## 项目配置

- **项目名称**: `dajia`
- **输出目录**: `dist`
- **构建命令**: `npm run build`

## 快速开始

```bash
# 1. 安装依赖（如果还没安装）
npm install

# 2. 一键部署
npm run deploy:cloudflare
```

就这么简单！ 🎉

## 查看部署状态

```bash
# 查看部署列表
wrangler pages deployment list --project-name=dajia

# 查看项目信息
wrangler pages project list
```

## 常见问题

### Q: 部署时提示需要 API Token？
A: 运行 `wrangler login` 进行登录，脚本会自动处理。

### Q: 如何查看部署日志？
A: 部署过程中会显示实时日志。完成后可以使用上述命令查看。

### Q: 如何更新环境变量？
A: 在 Cloudflare Dashboard 中设置，或在项目根目录创建 `.dev.vars` 文件（仅本地开发）。

## 相关链接

- Cloudflare Dashboard: https://dash.cloudflare.com
- 项目页面: https://dash.cloudflare.com/82ebeb1d91888e83e8e1b30eeb33d3c3/pages/view/dajia
